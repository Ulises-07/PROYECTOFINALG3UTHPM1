<?xml version="1.0" encoding="utf-8"?>
    <!-- Este archivo debe ir en la carpeta app/src/main/res/menu/ -->
    <!-- Si la carpeta 'menu' no existe, créala. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_buscar"
        android:icon="@android:drawable/ic_menu_search"
        android:title="Buscar Compañeros"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />

    <item
        android:id="@+id/menu_grupos"
        android:title="Crear Grupo"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_perfil"
        android:title="Mi Perfil"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_cerrar_sesion"
        android:title="Cerrar Sesión"
        app:showAsAction="never" />

</menu>
    <!--
    NOTA: Necesitarás agregar los íconos 'ic_add' (para el FAB) e 'ic_search' (para el menú).
    Haz clic derecho en 'res/drawable' -> New -> Vector Asset.
    Busca "add" y "search" en el Clip Art.
    -->